pipeline {
    agent any

    stages{
        stage('Backend - Running Unit Test'){
            steps{
                dir('bugtracker-backend'){
                    sh 'go test ./… -v'
                }
            }
        }
    }
}
